<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PumpMaui.SongSelectPage"
             Title="Song Select"
             BackgroundColor="#05010F"
             NavigationPage.HasNavigationBar="False">

    <Grid Padding="{OnPlatform Default=16, iOS=8, Android=8}" RowDefinitions="Auto,Auto,Auto,*,Auto">

        <!-- Header - Responsive sizing -->
        <VerticalStackLayout Grid.Row="0" Spacing="2" Margin="0,4,0,8">
            <Label Text="PUMP IT UP PHOENIX"
                   FontSize="{OnPlatform Default=28, iOS=22, Android=22}"
                   FontAttributes="Bold"
                   TextColor="#FFF4FF"
                   HorizontalTextAlignment="Center" />
            <Label Text="Select a song to play"
                   FontSize="{OnPlatform Default=14, iOS=12, Android=12}"
                   TextColor="#D0B7FF"
                   HorizontalTextAlignment="Center" />
        </VerticalStackLayout>

        <!-- Load buttons - Responsive layout -->
        <HorizontalStackLayout Grid.Row="1"
                               Spacing="8"
                               HorizontalOptions="Center"
                               Margin="0,0,0,12">
            <Button Text="Phoenix Songs"
                    Clicked="OnLoadDemoClicked"
                    BackgroundColor="#A100FF"
                    TextColor="White"
                    CornerRadius="10"
                    WidthRequest="{OnPlatform Default=160, iOS=140, Android=140}"
                    HeightRequest="{OnPlatform Default=44, iOS=40, Android=40}"
                    FontSize="{OnPlatform Default=14, iOS=12, Android=12}" />
            <Button Text="External Folder"
                    Clicked="OnOpenFolderClicked"
                    BackgroundColor="#5D42E6"
                    TextColor="White"
                    CornerRadius="10"
                    WidthRequest="{OnPlatform Default=160, iOS=140, Android=140}"
                    HeightRequest="{OnPlatform Default=44, iOS=40, Android=40}"
                    FontSize="{OnPlatform Default=14, iOS=12, Android=12}" />
        </HorizontalStackLayout>

        <!-- Scroll Speed Slider -->
        <Border Grid.Row="2"
                Background="#1A102A"
                Stroke="#4C327C"
                StrokeThickness="1"
                Padding="{OnPlatform Default=12, iOS=8, Android=8}"
                Margin="0,0,0,8"
                StrokeShape="RoundRectangle 8">
            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                <Label Grid.Column="0"
                       Text="Scroll Speed"
                       FontSize="{OnPlatform Default=14, iOS=12, Android=12}"
                       FontAttributes="Bold"
                       TextColor="#FCE3FF"
                       VerticalTextAlignment="Center" />
                <Slider Grid.Column="1"
                        x:Name="ScrollSpeedSlider"
                        Minimum="0.5"
                        Maximum="4.0"
                        Value="{Binding ScrollSpeed}"
                        ValueChanged="OnScrollSpeedChanged"
                        ThumbColor="#D94BFF"
                        MinimumTrackColor="#A100FF"
                        MaximumTrackColor="#4C327C" />
                <Label Grid.Column="2"
                       x:Name="ScrollSpeedLabel"
                       Text="{Binding ScrollSpeedText}"
                       FontSize="{OnPlatform Default=14, iOS=12, Android=12}"
                       FontAttributes="Bold"
                       TextColor="#FFE76A"
                       VerticalTextAlignment="Center"
                       MinimumWidthRequest="40"
                       HorizontalTextAlignment="Center" />
            </Grid>
        </Border>

        <!-- Song list -->
        <CollectionView Grid.Row="3"
                        x:Name="SongListView"
                        ItemsSource="{Binding SongList}"
                        SelectionMode="Single"
                        SelectionChanged="OnSongSelected">
            <CollectionView.EmptyView>
                <Label Text="Loading Phoenix song collection..."
                       TextColor="#8C76B5"
                       FontSize="{OnPlatform Default=14, iOS=12, Android=12}"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"
                       Margin="0,40" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,3"
                            Padding="{OnPlatform Default=12, iOS=8, Android=8}"
                            Stroke="#4C327C"
                            StrokeThickness="1"
                            Background="#1A102A"
                            StrokeShape="RoundRectangle 10">
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="{Binding Title}"
                                   FontAttributes="Bold"
                                   FontSize="{OnPlatform Default=16, iOS=14, Android=14}"
                                   TextColor="#FFF4FF" />
                            <Label Grid.Row="1" Grid.Column="0"
                                   Text="{Binding Artist}"
                                   FontSize="{OnPlatform Default=13, iOS=11, Android=11}"
                                   TextColor="#D0B7FF" />
                            <Label Grid.Row="2" Grid.Column="0"
                                   Text="{Binding ChartSummary}"
                                   FontSize="{OnPlatform Default=12, iOS=10, Android=10}"
                                   TextColor="#FFE76A" />
                            <Label Grid.Row="0" Grid.Column="1"
                                   Grid.RowSpan="3"
                                   Text="▶"
                                   FontSize="{OnPlatform Default=22, iOS=18, Android=18}"
                                   TextColor="#D94BFF"
                                   VerticalTextAlignment="Center"
                                   Margin="12,0,0,0" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Selected song / chart panel - Enhanced with chart display -->
        <Border Grid.Row="4"
                Background="#140A29"
                Stroke="#3E2B72"
                StrokeThickness="2"
                Padding="{OnPlatform Default=12, iOS=8, Android=8}"
                Margin="0,8,0,0"
                IsVisible="{Binding HasSelection}">
            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="12" RowSpacing="6">
                
                <!-- Song Info -->
                <VerticalStackLayout Grid.Column="0" Grid.Row="0" Spacing="2">
                    <Label x:Name="SelectedTitleLabel"
                           FontSize="{OnPlatform Default=18, iOS=16, Android=16}"
                           FontAttributes="Bold"
                           TextColor="#FFF4FF" />
                    <Label x:Name="SelectedArtistLabel"
                           FontSize="{OnPlatform Default=13, iOS=11, Android=11}"
                           TextColor="#D0B7FF" />
                </VerticalStackLayout>

                <!-- Chart Picker -->
                <VerticalStackLayout Grid.Column="0" Grid.Row="1" Spacing="4">
                    <Label Text="Chart" FontAttributes="Bold" TextColor="#FCE3FF" 
                           FontSize="{OnPlatform Default=12, iOS=10, Android=10}" />
                    <Picker x:Name="ChartPicker"
                            Title="Select chart"
                            SelectedIndexChanged="OnChartChanged"
                            TextColor="#FFF4FF"
                            TitleColor="#8C76B5"
                            FontSize="{OnPlatform Default=14, iOS=12, Android=12}"
                            HeightRequest="{OnPlatform Default=40, iOS=35, Android=35}" />
                </VerticalStackLayout>

                <!-- Selected Chart Display -->
                <Border Grid.Column="0" Grid.Row="2" 
                        Background="#2A1B4A"
                        Stroke="#D94BFF"
                        StrokeThickness="1"
                        Padding="8"
                        StrokeShape="RoundRectangle 6"
                        x:Name="SelectedChartBorder"
                        IsVisible="False">
                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                        <Border Grid.Column="0"
                                Background="#D94BFF"
                                Padding="4,2"
                                StrokeShape="RoundRectangle 3">
                            <Label x:Name="SelectedChartDifficultyLabel"
                                   Text="HARD"
                                   FontSize="{OnPlatform Default=10, iOS=9, Android=9}"
                                   FontAttributes="Bold"
                                   TextColor="White"
                                   HorizontalTextAlignment="Center" />
                        </Border>
                        
                        <Label Grid.Column="1"
                               x:Name="SelectedChartLevelLabel"
                               Text="Level 12"
                               FontSize="{OnPlatform Default=14, iOS=12, Android=12}"
                               FontAttributes="Bold"
                               TextColor="#FFE76A"
                               VerticalTextAlignment="Center" />
                        
                        <Label Grid.Column="2"
                               x:Name="SelectedChartNotesLabel"
                               Text="456 notes"
                               FontSize="{OnPlatform Default=12, iOS=10, Android=10}"
                               TextColor="#D0B7FF"
                               VerticalTextAlignment="Center" />
                    </Grid>
                </Border>

                <!-- Play Button -->
                <Button Grid.Column="1"
                        Grid.Row="0"
                        Grid.RowSpan="3"
                        x:Name="PlayButton"
                        Text="PLAY"
                        Clicked="OnPlayClicked"
                        BackgroundColor="#D94BFF"
                        TextColor="White"
                        FontAttributes="Bold"
                        FontSize="{OnPlatform Default=18, iOS=16, Android=16}"
                        CornerRadius="14"
                        WidthRequest="{OnPlatform Default=90, iOS=80, Android=80}"
                        HeightRequest="{OnPlatform Default=70, iOS=60, Android=60}"
                        VerticalOptions="Center" />
            </Grid>
        </Border>
    </Grid>
</ContentPage>